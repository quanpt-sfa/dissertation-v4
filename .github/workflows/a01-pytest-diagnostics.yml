name: A01 pytest diagnostics

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnostics:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
      - run: uv sync --all-extras
      - name: Capture pytest diagnostics
        run: |
          mkdir -p artifacts/a01-a15
          set +e
          uv run pytest -q > artifacts/a01-a15/pytest.log 2>&1
          echo "exit_code=$?" >> artifacts/a01-a15/pytest.log
          set -e
      - uses: actions/upload-artifact@v4
        with:
          name: a01-a15-pytest
          path: artifacts/a01-a15/pytest.log
          if-no-files-found: error
