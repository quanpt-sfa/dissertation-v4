decisions:
  D01: {statement: "The protocol has one configuration entry point.", config_anchors: [metadata.project], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [registry_lock]}
  D02: {statement: "Track A uses L1 and Gate 1 uses only allowed candidates.", config_anchors: [measurement.track_a_primary_endpoint], lock_stage: P0, enforced_by_steps: [P10], test_ids: [T001], output_evidence: [measurement_selection_registry]}
  D03: {statement: "Hierarchical-pi is sensitivity-only.", config_anchors: [measurement.hierarchical_pi.role], lock_stage: P0, enforced_by_steps: [P10], test_ids: [T001], output_evidence: [measurement_selection_registry]}
  D04: {statement: "AP is not evaluated against soft targets.", config_anchors: [evaluation.ap_soft_targets], lock_stage: P0, enforced_by_steps: [P11], test_ids: [T001], output_evidence: [model_freeze_receipt]}
  D05: {statement: "Content predictors are excluded from label models.", config_anchors: [features.label_model_allows_content], lock_stage: P0, enforced_by_steps: [P11], test_ids: [T001], output_evidence: [model_freeze_receipt]}
  D06: {statement: "Missing source is not evidence zero.", config_anchors: [evidence.missing_is_zero], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [registry_lock]}
  D07: {statement: "Immature follow-up is not a negative outcome.", config_anchors: [evidence.immature_is_negative], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [registry_lock]}
  D08: {statement: "Initial fold is excluded from confirmatory pooling.", config_anchors: [folds.initial_in_confirmatory_pool], lock_stage: P0, enforced_by_steps: [P11], test_ids: [T001], output_evidence: [model_freeze_receipt]}
  D09: {statement: "Prospective fold is excluded from retrospective evaluation.", config_anchors: [folds.prospective_in_retrospective_evaluation], lock_stage: P0, enforced_by_steps: [P11], test_ids: [T001], output_evidence: [model_freeze_receipt]}
  D10: {statement: "Known cases remain sealed until P15.", config_anchors: [known_cases.opening_step], lock_stage: P0, enforced_by_steps: [P15], test_ids: [T001], output_evidence: [known_cases_seal]}
  D11: {statement: "Outer outcomes require model freeze.", config_anchors: [access_control.outer_outcomes_open_after], lock_stage: P0, enforced_by_steps: [P11], test_ids: [T001], output_evidence: [model_freeze_receipt]}
  D12: {statement: "Full-sample weights are descriptive only.", config_anchors: [weighting.full_sample_role], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [registry_lock]}
  D13: {statement: "Analytical weights are development-history fitted.", config_anchors: [weighting.analytical_fit_scope], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [registry_lock]}
  D14: {statement: "IPCW is sensitivity only.", config_anchors: [weighting.ipcw_role], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [registry_lock]}
  D15: {statement: "The benchmark ladder has six levels and PU is separate.", config_anchors: [evaluation.benchmark_ladder], lock_stage: P0, enforced_by_steps: [P11], test_ids: [T001], output_evidence: [model_freeze_receipt]}
  D16: {statement: "Strict selection removes held-out channel information.", config_anchors: [measurement.strict_selection], lock_stage: P0, enforced_by_steps: [P10], test_ids: [T001], output_evidence: [measurement_selection_registry]}
  D17: {statement: "Fixed-pi reports exclude pi bias and RMSE.", config_anchors: [measurement.fixed_pi], lock_stage: P0, enforced_by_steps: [P10], test_ids: [T001], output_evidence: [measurement_selection_registry]}
  D18: {statement: "Hierarchical-pi parameter bias is limited to estimated parameters.", config_anchors: [measurement.hierarchical_pi.estimated_parameter_bias_only], lock_stage: P0, enforced_by_steps: [P10], test_ids: [T001], output_evidence: [measurement_selection_registry]}
  D19: {statement: "CHNC3 inference is family adjusted.", config_anchors: [inference.chnc3_family_adjusted], lock_stage: P0, enforced_by_steps: [P17], test_ids: [T001], output_evidence: [final_report]}
  D20: {statement: "Utility costs remain separate.", config_anchors: [utility.components], lock_stage: P0, enforced_by_steps: [P17], test_ids: [T001], output_evidence: [final_report]}
  D21: {statement: "Simulation imports production labels.", config_anchors: [simulation.imports_production_labels], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [capability_seed]}
  D22: {statement: "Simulation replication and MCSE targets are locked.", config_anchors: [simulation.minimum_replications], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [capability_seed]}
  D23: {statement: "Simulation roster contains required model classes.", config_anchors: [simulation.model_roster], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [capability_seed]}
  D24: {statement: "Simulation includes Gate 3 operating characteristics.", config_anchors: [simulation.gate3_operating_characteristics], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [capability_seed]}
  D25: {statement: "Shared ledger append operations are forbidden.", config_anchors: [reproducibility.shared_ledger_append], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [registry_lock]}
  D26: {statement: "Physical columns have one owned registry.", config_anchors: [columns.firm_id], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [schema_catalog]}
  D27: {statement: "Artifact paths are centrally declared.", config_anchors: [artifacts.registry_lock], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [artifact_catalog]}
  D28: {statement: "Step access is centrally declared.", config_anchors: [steps.P00], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [access_matrix]}
  D29: {statement: "Schema contracts resolve logical columns.", config_anchors: [schemas.registry_schema], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [schema_catalog]}
  D30: {statement: "Status vocabulary is registered.", config_anchors: [vocabulary.statuses], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [registry_lock]}
  D31: {statement: "Declared channels are controlled by configuration.", config_anchors: [data_sources.declared_channels], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [registry_lock]}
  D32: {statement: "Maturity is required for risk sets.", config_anchors: [risksets.maturity_required], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [registry_lock]}
  D33: {statement: "Calibration uses development cross-fitting.", config_anchors: [calibration.development_cross_fitted_only], lock_stage: P0, enforced_by_steps: [P11], test_ids: [T001], output_evidence: [model_freeze_receipt]}
  D34: {statement: "Learners are registered configuration objects.", config_anchors: [learners.registered], lock_stage: P0, enforced_by_steps: [P11], test_ids: [T001], output_evidence: [model_freeze_receipt]}
  D35: {statement: "Domains are centrally registered.", config_anchors: [domains.registry], lock_stage: P0, enforced_by_steps: [P17], test_ids: [T001], output_evidence: [final_report]}
  D36: {statement: "Reporting catalogs are generated from configuration.", config_anchors: [reporting.generated_catalogs], lock_stage: P0, enforced_by_steps: [P17], test_ids: [T001], output_evidence: [final_report]}
  D37: {statement: "Agent rules are generated from their owned module.", config_anchors: [agent_contract.rules], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [p00_audit_report]}
  D38: {statement: "Configuration sources are versioned protocol metadata.", config_anchors: [metadata.protocol_version], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [source_config_manifest]}
  D39: {statement: "Entity resolution is registered before later stages.", config_anchors: [entity_resolution.policy], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [registry_lock]}
  D40: {statement: "Reason codes are centrally registered.", config_anchors: [vocabulary.reason_codes], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [registry_lock]}
  D41: {statement: "Known-case identifiers are sealed configuration content.", config_anchors: [known_cases.content_ids], lock_stage: P0, enforced_by_steps: [P15], test_ids: [T001], output_evidence: [known_case_results]}
  D42: {statement: "The configuration hash algorithm is declared.", config_anchors: [reproducibility.hash_algorithm], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [protocol_hash]}
  D43: {statement: "Track A policy is registered.", config_anchors: [study.track_a], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [registry_lock]}
  D44: {statement: "Generated documentation remains reproducible.", config_anchors: [reporting.generated_catalogs], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [p00_audit_report]}
  D45: {statement: "P0 publishes an immutable locked registry.", config_anchors: [artifacts.registry_lock.immutability], lock_stage: P0, enforced_by_steps: [P00], test_ids: [T001], output_evidence: [registry_lock]}
